from django.contrib import admin
from django.urls import path, include

# from .views import (ClubListView, ClubDetailView, ClubCreateView, ClubUpdateView,
#                     ClubDeleteView, ClubCloseView, ClubCardView, ClubJoinListView,
#                     ClubQuitListView, ClubJoinApplyListView, ClubJoinApplyCreateView,
#                     ClubJoinApplyUpdateView, ClubJoinApplyDeleteView, ClubJoinApplyFullListView,
#                     ClubJoinPermitCreateView, ClubJoinPermitUpdateView, ClubJoinPermitDeleteView
#                     )

# "/club"

urlpatterns = [
    # path('',ClubListView.as_view(), name="club"),
    # path('view/<int:pk>',ClubDetailView.as_view(), name="club"),
    # path('new/',ClubCreateView.as_view(), name="club"),
    # path('edit/<int:pk>/',ClubUpdateView.as_view(), name="club"),
    # path('del/<int:pk>/',ClubDeleteView.as_view(), name="club"),
    # path('close/<int:pk>/',ClubCloseView.as_view(), name="club"),
    # path('card/',ClubCardView.as_view(), name="club"),
    # path('available/join',ClubJoinListView.as_view(), name="club"),
    # path('available/quit',ClubQuitListView.as_view(), name="club"),
    # path('join/apply',ClubJoinApplyListView.as_view(), name="club"),
    # path('join/apply/new',ClubJoinApplyCreateView.as_view(), name="club"),
    # path('',ClubJoinApplyUpdateView.as_view(), name="club"),
    # path('',ClubJoinApplyDeleteView.as_view(), name="club"),
    # path('',ClubJoinApplyFullListView.as_view(), name="club"),
    # path('',ClubJoinPermitCreateView.as_view(), name="club"),
    # path('',ClubJoinPermitUpdateView.as_view(), name="club"),
    # path('',ClubJoinPermitDeleteView.as_view(), name="club"),
]
